{
  "scripts": {
    "start": "babel-node index --presets=env"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "config": "^3.3.6",
    "dotenv": "^10.0.0",
    "node-telegram-bot-api": "^0.56.0"
  }
}
